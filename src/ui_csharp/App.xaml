<Application x:Class="Pcsx5Ui.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!-- Modern Windows 11 style global resources -->
        <SolidColorBrush x:Key="SystemAccentBrush" Color="#0078D4"/>
        <SolidColorBrush x:Key="GlassBackgroundBrush" Color="#AA111115"/>
        <SolidColorBrush x:Key="ForegroundWhiteBrush" Color="#FFFFFFFF"/>
        <SolidColorBrush x:Key="ForegroundMutedBrush" Color="#FFA0A0A5"/>
        <SolidColorBrush x:Key="BorderLightBrush" Color="#1EFFFFFF"/>
        
        <!-- Type ramp.  The shell is a fullscreen console dashboard read at
             distance, so the base of this ramp is deliberately larger than a
             desktop tool would use.  Every FontSize references a token here,
             so the whole interface is retuned from this one block. -->
        <sys:Double x:Key="TypeXS">12</sys:Double>
        <sys:Double x:Key="TypeS">13</sys:Double>
        <sys:Double x:Key="TypeM">14</sys:Double>
        <sys:Double x:Key="TypeML">15</sys:Double>
        <sys:Double x:Key="TypeL">16</sys:Double>
        <sys:Double x:Key="TypeXL">17</sys:Double>
        <sys:Double x:Key="Type2XL">18</sys:Double>
        <sys:Double x:Key="Type3XL">20</sys:Double>
        <sys:Double x:Key="Type4XL">22</sys:Double>
        <sys:Double x:Key="Type5XL">24</sys:Double>
        <sys:Double x:Key="TypeDisplayS">28</sys:Double>
        <sys:Double x:Key="TypeDisplayM">36</sys:Double>
        <sys:Double x:Key="TypeDisplayL">44</sys:Double>

        <!-- Focus ring. The default WPF focus adornment is a dotted rectangle
             that disappears against this palette; a controller user has to be
             able to see what is selected from across a room. -->
        <Style x:Key="AccentFocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle Margin="-4" StrokeThickness="3"
                                   Stroke="{DynamicResource PickerAccentBrush}"
                                   RadiusX="10" RadiusY="10"
                                   SnapsToDevicePixels="True"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- In-app folder browser palette -->
        <SolidColorBrush x:Key="PickerSurfaceBrush" Color="#1C1F26"/>
        <SolidColorBrush x:Key="PickerListBrush" Color="#12151B"/>
        <SolidColorBrush x:Key="PickerBorderBrush" Color="#252A33"/>
        <SolidColorBrush x:Key="PickerAccentBrush" Color="#0099FF"/>
        <SolidColorBrush x:Key="PickerTextBrush" Color="#FFFFFF"/>
        <!-- #B8B8C0 on #1C1F26 clears 7:1 contrast; the older #A0A0A5 did not. -->
        <SolidColorBrush x:Key="PickerMutedBrush" Color="#B8B8C0"/>

        <!-- Modern ScrollViewer style -->
        <Style x:Key="ModernScrollViewer" TargetType="{x:Type ScrollViewer}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollViewer}">
                        <Grid Background="{TemplateBinding Background}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <ScrollContentPresenter x:Name="PART_ScrollContentPresenter" CanContentScroll="{TemplateBinding CanContentScroll}" CanHorizontallyScroll="False" CanVerticallyScroll="False" ContentTemplate="{TemplateBinding ContentTemplate}" Content="{TemplateBinding Content}" Grid.Column="0" Margin="{TemplateBinding Padding}" Grid.Row="0"/>
                            <ScrollBar x:Name="PART_VerticalScrollBar" AutomationProperties.Name="VerticalScrollBar" Cursor="Arrow" Grid.Column="1" Maximum="{TemplateBinding ScrollableHeight}" Minimum="0" Grid.Row="0" Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" Value="{Binding VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}" Width="8"/>
                            <ScrollBar x:Name="PART_HorizontalScrollBar" AutomationProperties.Name="HorizontalScrollBar" Cursor="Arrow" Grid.Column="0" Maximum="{TemplateBinding ScrollableWidth}" Minimum="0" Grid.Row="1" Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}" Value="{Binding HorizontalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}" Height="8"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>
