add_library(pcsx5_execution STATIC src/interpreter.cpp src/trace.cpp src/worker_protocol.cpp src/native_run.cpp src/arm64_jit.cpp src/arm64_step.cpp)
target_compile_features(pcsx5_execution PUBLIC cxx_std_23)
target_include_directories(pcsx5_execution PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
target_link_libraries(pcsx5_execution PUBLIC pcsx5_core pcsx5_runtime)
pcsx5_enable_warnings(pcsx5_execution)
add_executable(pcsx5_execution_worker "${CMAKE_SOURCE_DIR}/tools/worker/main.cpp")
target_link_libraries(pcsx5_execution_worker PRIVATE pcsx5_execution)
pcsx5_enable_warnings(pcsx5_execution_worker)

if(CMAKE_SYSTEM_PROCESSOR MATCHES "^(x86_64|AMD64|amd64)$")
    if(WIN32)
        add_library(pcsx5_native_step STATIC ../runtime/src/windows_native_step.cpp)
    elseif(CMAKE_SYSTEM_NAME STREQUAL "Linux")
        add_library(pcsx5_native_step STATIC ../runtime/src/linux_native_step.cpp)
    endif()
    if(TARGET pcsx5_native_step)
        target_link_libraries(pcsx5_native_step PUBLIC pcsx5_execution)
        pcsx5_enable_warnings(pcsx5_native_step)
        add_executable(pcsx5_native_probe ../tools/worker/native_probe.cpp)
        target_link_libraries(pcsx5_native_probe PRIVATE pcsx5_native_step)
        if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
            target_compile_definitions(pcsx5_native_probe PRIVATE PCSX5_NATIVE_LINUX=1)
        endif()
        pcsx5_enable_warnings(pcsx5_native_probe)
        add_executable(pcsx5_native_cost ../tools/worker/native_cost.cpp)
        target_link_libraries(pcsx5_native_cost PRIVATE pcsx5_native_step)
        if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
            target_compile_definitions(pcsx5_native_cost PRIVATE PCSX5_NATIVE_LINUX=1)
        endif()
        pcsx5_enable_warnings(pcsx5_native_cost)
    endif()
endif()
