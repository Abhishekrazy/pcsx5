# PCSX5 Rule

C++20 is the baseline. Prefer RAII, explicit ownership, fixed-width guest types, no global mutable state, no exceptions across C ABI, explicit guest/host pointer distinction and measured optimization. C++26 requires an ADR and concrete benefit.
