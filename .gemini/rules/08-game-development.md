# PCSX5 Rule

Use game-development reasoning: assets, loading, threads, frame timing, rendering, shaders, input, audio, save/config and lifecycle. Do not automatically blame the emulator; identify the first responsible layer.
