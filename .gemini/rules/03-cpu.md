# PCSX5 Rule

Guest execution is x86_64 direct execution plus controlled dispatcher/ABI support. Do not introduce a JIT without evidence. Preserve register, ABI, fault and TLS behavior. MASM remains narrowly scoped and documented.
