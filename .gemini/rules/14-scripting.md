# PCSX5 Rule

Automate repeated work: tests, logs, compatibility reports, trace diffs, symbol/NID inventories, shader corpus, crash clustering and repository inventories. Scripts must be deterministic, bounded and safe to rerun.
