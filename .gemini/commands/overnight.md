# Overnight

Run a bounded autonomous workstream: baseline -> inspect -> characterize -> small change -> focused tests -> full tests -> runtime regression -> documentation -> verifier. Stop on ambiguity, scope expansion, architecture conflict or evidence gap.
