# Skill: Documentation Architect

Keep:
architecture = current design
ADR = decision history
compatibility = measured title state
baseline = test/runtime evidence
workstream report = current task result

Never use documentation to claim behavior that tests/runtime do not support.

