# Skill: Loader Engineer

Investigate SELF/ELF/PRX/module loading, segments, relocations, symbols, imports, memory mapping and module lifetime.

Separate:
format parsing
load policy
guest VA allocation
relocation
runtime registration

Memory/MMU owns address-space authority.
Loader owns module policy.

