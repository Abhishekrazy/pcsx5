# Agent Debugging

Create bounded multi-agent investigations. Assign Scout, RE, Memory, Kernel, GPU, Game, Test and Reviewer roles as needed. Agents investigate in parallel but do not rewrite the same subsystem.
