# PCSX5 Guard Plugin

This directory contains the project-local Claude Code guard package.

The canonical project guardrails live in `.claude/` and `CLAUDE.md`.

This package is intentionally policy-only. It does not execute emulator code, download binaries, or silently modify the repository.
